fonctionA(int x, int y);
fonctionB(int x, float f);

int main(void)
{
    int x, y, z;

    z = fonctionA(x,y);
    return (0);
}
void fonctionB (int x, float f)
{
    /*
     *
     * */
}
int fonctionA (int x, int y)
{
    int z;
    float a;
    /*
     *
     * */
    fonctionB(x,a);
}
